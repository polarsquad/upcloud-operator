---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: loadbalancerfrontendrules.loadbalancer.upcloud.polarsquad.com
spec:
  group: loadbalancer.upcloud.polarsquad.com
  names:
    kind: LoadBalancerFrontendRule
    listKind: LoadBalancerFrontendRuleList
    plural: loadbalancerfrontendrules
    singular: loadbalancerfrontendrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoadBalancerFrontendRule is a load balancer frontend rule.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LoadBalancerFrontendRuleSpec defines the desired state of
              a frontend rule.
            properties:
              actions:
                description: Actions are evaluated in order.
                items:
                  description: |-
                    RuleAction is one action on a rule. Type selects which sub-struct is used;
                    exactly one sub-struct matching Type may be set (enforced by CEL).
                  properties:
                    httpRedirect:
                      description: ActionHTTPRedirect is an "http_redirect" action.
                      properties:
                        location:
                          type: string
                        scheme:
                          enum:
                          - http
                          - https
                          type: string
                        status:
                          type: integer
                      required:
                      - location
                      type: object
                    httpReturn:
                      description: ActionHTTPReturn is an "http_return" action.
                      properties:
                        contentType:
                          type: string
                        payload:
                          type: string
                        status:
                          type: integer
                      required:
                      - status
                      type: object
                    httpRewritePath:
                      description: ActionHTTPRewritePath is an "http_rewrite_path"
                        action.
                      properties:
                        matchPattern:
                          type: string
                        rewriteTo:
                          type: string
                      required:
                      - matchPattern
                      - rewriteTo
                      type: object
                    httpRewriteURI:
                      description: ActionHTTPRewriteURI is an "http_rewrite_uri" action.
                      properties:
                        matchPattern:
                          type: string
                        rewriteTo:
                          type: string
                      required:
                      - matchPattern
                      - rewriteTo
                      type: object
                    setRequestHeader:
                      description: ActionSetHeader is a "set_request_header" or "set_response_header"
                        action.
                      properties:
                        header:
                          type: string
                        value:
                          type: string
                      required:
                      - header
                      - value
                      type: object
                    setResponseHeader:
                      description: ActionSetHeader is a "set_request_header" or "set_response_header"
                        action.
                      properties:
                        header:
                          type: string
                        value:
                          type: string
                      required:
                      - header
                      - value
                      type: object
                    type:
                      enum:
                      - use_backend
                      - tcp_reject
                      - http_return
                      - http_redirect
                      - http_rewrite_path
                      - http_rewrite_uri
                      - set_forwarded_headers
                      - set_request_header
                      - set_response_header
                      type: string
                    useBackend:
                      description: ActionUseBackend is a "use_backend" action.
                      properties:
                        backend:
                          description: Backend is the name of a LoadBalancerBackend.
                          type: string
                      required:
                      - backend
                      type: object
                  required:
                  - type
                  type: object
                type: array
              deletionPolicy:
                default: Delete
                description: DeletionPolicy decides what happens to the UpCloud resource
                  when the CR is deleted.
                enum:
                - Delete
                - Orphan
                type: string
              frontendRef:
                description: FrontendRef points at the owning LoadBalancerFrontend.
                  Immutable.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: frontendRef is immutable
                  rule: self == oldSelf
              matchers:
                description: Matchers is the set of matchers.
                items:
                  description: |-
                    RuleMatcher is one matcher on a rule. Type selects which sub-struct is used;
                    exactly one sub-struct matching Type may be set (enforced by CEL).
                  properties:
                    bodySize:
                      description: |-
                        MatcherInteger is a matcher over an integer (src_port, body_size,
                        http_status).
                      properties:
                        method:
                          enum:
                          - equal
                          - greater
                          - greater_or_equal
                          - less
                          - less_or_equal
                          - range
                          type: string
                        rangeEnd:
                          type: integer
                        rangeStart:
                          type: integer
                        value:
                          type: integer
                      required:
                      - method
                      - value
                      type: object
                    cookie:
                      description: |-
                        MatcherStringWithArgument is a matcher over a named string (cookie, header,
                        request_header, response_header, url_param).
                      properties:
                        ignoreCase:
                          type: boolean
                        method:
                          enum:
                          - exact
                          - substring
                          - regexp
                          - starts
                          - ends
                          - domain
                          - ip
                          - exists
                          type: string
                        name:
                          type: string
                        value:
                          type: string
                      required:
                      - method
                      - name
                      - value
                      type: object
                    header:
                      description: |-
                        MatcherStringWithArgument is a matcher over a named string (cookie, header,
                        request_header, response_header, url_param).
                      properties:
                        ignoreCase:
                          type: boolean
                        method:
                          enum:
                          - exact
                          - substring
                          - regexp
                          - starts
                          - ends
                          - domain
                          - ip
                          - exists
                          type: string
                        name:
                          type: string
                        value:
                          type: string
                      required:
                      - method
                      - name
                      - value
                      type: object
                    host:
                      description: MatcherHost is a "host" matcher.
                      properties:
                        value:
                          type: string
                      required:
                      - value
                      type: object
                    httpMethod:
                      description: MatcherHTTPMethod is a "http_method" matcher.
                      properties:
                        value:
                          type: string
                      required:
                      - value
                      type: object
                    httpStatus:
                      description: |-
                        MatcherInteger is a matcher over an integer (src_port, body_size,
                        http_status).
                      properties:
                        method:
                          enum:
                          - equal
                          - greater
                          - greater_or_equal
                          - less
                          - less_or_equal
                          - range
                          type: string
                        rangeEnd:
                          type: integer
                        rangeStart:
                          type: integer
                        value:
                          type: integer
                      required:
                      - method
                      - value
                      type: object
                    inverse:
                      type: boolean
                    numMembersUp:
                      description: MatcherNumMembersUp is a "num_members_up" matcher.
                      properties:
                        backend:
                          description: Backend is the name of a LoadBalancerBackend.
                          type: string
                        method:
                          enum:
                          - equal
                          - greater
                          - greater_or_equal
                          - less
                          - less_or_equal
                          - range
                          type: string
                        value:
                          type: integer
                      required:
                      - backend
                      - method
                      - value
                      type: object
                    path:
                      description: MatcherString is a matcher over a string (path,
                        url, url_query).
                      properties:
                        ignoreCase:
                          type: boolean
                        method:
                          enum:
                          - exact
                          - substring
                          - regexp
                          - starts
                          - ends
                          - domain
                          - ip
                          - exists
                          type: string
                        value:
                          type: string
                      required:
                      - method
                      - value
                      type: object
                    requestHeader:
                      description: |-
                        MatcherStringWithArgument is a matcher over a named string (cookie, header,
                        request_header, response_header, url_param).
                      properties:
                        ignoreCase:
                          type: boolean
                        method:
                          enum:
                          - exact
                          - substring
                          - regexp
                          - starts
                          - ends
                          - domain
                          - ip
                          - exists
                          type: string
                        name:
                          type: string
                        value:
                          type: string
                      required:
                      - method
                      - name
                      - value
                      type: object
                    responseHeader:
                      description: |-
                        MatcherStringWithArgument is a matcher over a named string (cookie, header,
                        request_header, response_header, url_param).
                      properties:
                        ignoreCase:
                          type: boolean
                        method:
                          enum:
                          - exact
                          - substring
                          - regexp
                          - starts
                          - ends
                          - domain
                          - ip
                          - exists
                          type: string
                        name:
                          type: string
                        value:
                          type: string
                      required:
                      - method
                      - name
                      - value
                      type: object
                    srcIP:
                      description: MatcherSrcIP is a "src_ip" matcher.
                      properties:
                        value:
                          type: string
                      required:
                      - value
                      type: object
                    srcPort:
                      description: |-
                        MatcherInteger is a matcher over an integer (src_port, body_size,
                        http_status).
                      properties:
                        method:
                          enum:
                          - equal
                          - greater
                          - greater_or_equal
                          - less
                          - less_or_equal
                          - range
                          type: string
                        rangeEnd:
                          type: integer
                        rangeStart:
                          type: integer
                        value:
                          type: integer
                      required:
                      - method
                      - value
                      type: object
                    type:
                      enum:
                      - src_ip
                      - src_port
                      - body_size
                      - path
                      - url
                      - url_query
                      - host
                      - http_method
                      - http_status
                      - cookie
                      - header
                      - request_header
                      - response_header
                      - url_param
                      - num_members_up
                      type: string
                    url:
                      description: MatcherString is a matcher over a string (path,
                        url, url_query).
                      properties:
                        ignoreCase:
                          type: boolean
                        method:
                          enum:
                          - exact
                          - substring
                          - regexp
                          - starts
                          - ends
                          - domain
                          - ip
                          - exists
                          type: string
                        value:
                          type: string
                      required:
                      - method
                      - value
                      type: object
                    urlParam:
                      description: |-
                        MatcherStringWithArgument is a matcher over a named string (cookie, header,
                        request_header, response_header, url_param).
                      properties:
                        ignoreCase:
                          type: boolean
                        method:
                          enum:
                          - exact
                          - substring
                          - regexp
                          - starts
                          - ends
                          - domain
                          - ip
                          - exists
                          type: string
                        name:
                          type: string
                        value:
                          type: string
                      required:
                      - method
                      - name
                      - value
                      type: object
                    urlQuery:
                      description: MatcherString is a matcher over a string (path,
                        url, url_query).
                      properties:
                        ignoreCase:
                          type: boolean
                        method:
                          enum:
                          - exact
                          - substring
                          - regexp
                          - starts
                          - ends
                          - domain
                          - ip
                          - exists
                          type: string
                        value:
                          type: string
                      required:
                      - method
                      - value
                      type: object
                  required:
                  - type
                  type: object
                type: array
              matchingCondition:
                default: and
                description: MatchingCondition combines the matchers; defaults to
                  "and".
                enum:
                - and
                - or
                type: string
              name:
                description: Name of the rule in UpCloud. Immutable.
                maxLength: 64
                pattern: ^[a-zA-Z0-9_-]+$
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              priority:
                description: 'Priority: 0-100, higher is evaluated first.'
                maximum: 100
                minimum: 0
                type: integer
            required:
            - frontendRef
            - name
            - priority
            type: object
          status:
            description: |-
              LoadBalancerFrontendRuleStatus defines the observed state of
              LoadBalancerFrontendRule.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              frontendName:
                description: FrontendName is the owning frontend's name in UpCloud.
                type: string
              name:
                description: Name of the rule in UpCloud.
                type: string
              serviceUUID:
                description: UUID of the parent load balancer.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
