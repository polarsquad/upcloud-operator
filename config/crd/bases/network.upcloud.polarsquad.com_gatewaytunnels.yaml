---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: gatewaytunnels.network.upcloud.polarsquad.com
spec:
  group: network.upcloud.polarsquad.com
  names:
    kind: GatewayTunnel
    listKind: GatewayTunnelList
    plural: gatewaytunnels
    singular: gatewaytunnel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.connectionUUID
      name: Connection
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GatewayTunnel is an UpCloud gateway IPSec VPN tunnel.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GatewayTunnelSpec defines the desired state of a gateway
              tunnel.
            properties:
              connectionRef:
                description: ConnectionRef references the parent GatewayConnection.
                  Immutable.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: connectionRef is immutable
                  rule: self == oldSelf
              deletionPolicy:
                default: Delete
                description: DeletionPolicy decides what happens to the UpCloud resource
                  when the CR is deleted.
                enum:
                - Delete
                - Orphan
                type: string
              ipsec:
                description: GatewayTunnelIPSec is the tunnel's IPSec configuration.
                properties:
                  authentication:
                    description: |-
                      GatewayTunnelIPSecAuth holds the tunnel's IPSec authentication. The PSK is
                      read from a Secret at create time only; the API never returns it, so PSK
                      drift is undetectable.
                    properties:
                      pskSecretRef:
                        description: PskSecretRef selects the pre-shared key from
                          a Secret.
                        properties:
                          key:
                            minLength: 1
                            type: string
                          name:
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      type:
                        default: psk
                        enum:
                        - psk
                        type: string
                    required:
                    - pskSecretRef
                    type: object
                  childRekeyTime:
                    type: integer
                  dpdDelay:
                    type: integer
                  dpdTimeout:
                    type: integer
                  ikeLifetime:
                    type: integer
                  phase1Algorithms:
                    items:
                      type: string
                    type: array
                  phase1DhGroupNumbers:
                    items:
                      type: integer
                    type: array
                  phase1IntegrityAlgorithms:
                    items:
                      type: string
                    type: array
                  phase2Algorithms:
                    items:
                      type: string
                    type: array
                  phase2DhGroupNumbers:
                    items:
                      type: integer
                    type: array
                  phase2IntegrityAlgorithms:
                    items:
                      type: string
                    type: array
                  rekeyTime:
                    type: integer
                required:
                - authentication
                type: object
              localAddressName:
                description: LocalAddressName must match a Gateway.status.addresses[].name.
                minLength: 1
                type: string
              name:
                description: Name in UpCloud. Defaults to metadata.name. Immutable.
                maxLength: 255
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              remoteAddress:
                description: RemoteAddress is the peer's public IP.
                minLength: 1
                type: string
            required:
            - connectionRef
            - localAddressName
            - remoteAddress
            type: object
          status:
            description: GatewayTunnelStatus defines the observed state of GatewayTunnel.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectionUUID:
                type: string
              gatewayUUID:
                type: string
              operationalState:
                type: string
              uuid:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
