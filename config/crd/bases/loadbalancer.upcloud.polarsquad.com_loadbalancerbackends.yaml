---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: loadbalancerbackends.loadbalancer.upcloud.polarsquad.com
spec:
  group: loadbalancer.upcloud.polarsquad.com
  names:
    kind: LoadBalancerBackend
    listKind: LoadBalancerBackendList
    plural: loadbalancerbackends
    singular: loadbalancerbackend
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoadBalancerBackend is a load balancer backend.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LoadBalancerBackendSpec defines the desired state of a load balancer
              backend.
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy decides what happens to the UpCloud resource
                  when the CR is deleted.
                enum:
                - Delete
                - Orphan
                type: string
              loadBalancerRef:
                description: LoadBalancerRef points at the owning LoadBalancer. Immutable.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: loadBalancerRef is immutable
                  rule: self == oldSelf
              name:
                description: Name of the backend in UpCloud. Immutable.
                maxLength: 64
                pattern: ^[a-zA-Z0-9_-]+$
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              properties:
                description: |-
                  LoadBalancerBackendProperties mirrors upcloud.LoadBalancerBackendProperties.
                  Only the fields the user sets are sent (pointers are nil when unset).
                properties:
                  healthCheckExpectedStatus:
                    minimum: 0
                    type: integer
                  healthCheckFall:
                    minimum: 0
                    type: integer
                  healthCheckInterval:
                    minimum: 0
                    type: integer
                  healthCheckRise:
                    minimum: 0
                    type: integer
                  healthCheckTLSVerify:
                    type: boolean
                  healthCheckType:
                    enum:
                    - "off"
                    - tcp
                    - http
                    type: string
                  healthCheckURL:
                    type: string
                  http2Enabled:
                    type: boolean
                  outboundProxyProtocol:
                    enum:
                    - v1
                    - v2
                    type: string
                  stickySessionCookieName:
                    type: string
                  timeoutServer:
                    minimum: 0
                    type: integer
                  timeoutTunnel:
                    minimum: 0
                    type: integer
                  tlsEnabled:
                    type: boolean
                  tlsUseSystemCA:
                    type: boolean
                  tlsVerify:
                    type: boolean
                type: object
              resolverRef:
                description: ResolverRef points at a LoadBalancerResolver; its status.name
                  is used.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - loadBalancerRef
            type: object
          status:
            description: LoadBalancerBackendStatus defines the observed state of LoadBalancerBackend.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              name:
                description: Name of the backend in UpCloud.
                type: string
              serviceUUID:
                description: UUID of the parent load balancer.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
