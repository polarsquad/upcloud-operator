---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: objectstorageusers.objectstorage.upcloud.polarsquad.com
spec:
  group: objectstorage.upcloud.polarsquad.com
  names:
    kind: ObjectStorageUser
    listKind: ObjectStorageUserList
    plural: objectstorageusers
    singular: objectstorageuser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.username
      name: Username
      type: string
    - jsonPath: .status.arn
      name: ARN
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ObjectStorageUser is an IAM-style user of an object storage service.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ObjectStorageUserSpec defines the desired state of an object
              storage user.
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy decides what happens to the UpCloud resource
                  when the CR is deleted.
                enum:
                - Delete
                - Orphan
                type: string
              policies:
                description: |-
                  Policies are the names of ObjectStoragePolicy resources in the same
                  service to attach to this user. Each may also be given via PolicyRefs.
                items:
                  type: string
                maxItems: 64
                type: array
              policyRefs:
                description: |-
                  PolicyRefs point at ObjectStoragePolicy CRs in the same namespace;
                  their status.name is attached to the user.
                items:
                  description: LocalObjectReference names an object of a known kind
                    in the same namespace.
                  properties:
                    name:
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                maxItems: 64
                type: array
              serviceRef:
                description: |-
                  ServiceRef points at the ManagedObjectStorage the user belongs to.
                  Immutable.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: serviceRef is immutable
                  rule: self == oldSelf
              username:
                description: Username in UpCloud. Defaults to metadata.name. Immutable.
                maxLength: 64
                pattern: ^[a-zA-Z0-9_-]+$
                type: string
                x-kubernetes-validations:
                - message: username is immutable
                  rule: self == oldSelf
            required:
            - serviceRef
            type: object
          status:
            description: ObjectStorageUserStatus defines the observed state of ObjectStorageUser.
            properties:
              arn:
                description: ARN of the user in UpCloud.
                type: string
              attachedPolicies:
                description: AttachedPolicies are the policy names currently attached
                  in UpCloud.
                items:
                  type: string
                type: array
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              serviceUUID:
                description: ServiceUUID of the owning service.
                type: string
              username:
                description: Username as stored in UpCloud.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
