---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: objectstorageaccesskeys.objectstorage.upcloud.polarsquad.com
spec:
  group: objectstorage.upcloud.polarsquad.com
  names:
    kind: ObjectStorageAccessKey
    listKind: ObjectStorageAccessKeyList
    plural: objectstorageaccesskeys
    singular: objectstorageaccesskey
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.accessKeyID
      name: AccessKeyID
      type: string
    - jsonPath: .spec.status
      name: Status
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ObjectStorageAccessKey is an S3-style access key for an object storage
          user. The secret is written to a Secret in the same namespace once, at
          create time.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ObjectStorageAccessKeySpec defines the desired state of an object storage
              access key (an S3-style secret key pair for a user).
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy decides what happens to the UpCloud resource
                  when the CR is deleted.
                enum:
                - Delete
                - Orphan
                type: string
              secretName:
                description: |-
                  SecretName is the name of the Secret holding the S3 credentials.
                  Defaults to <metadata.name>-s3.
                maxLength: 253
                type: string
              status:
                default: Active
                description: Status of the key, Active (default) or Inactive.
                enum:
                - Active
                - Inactive
                type: string
              userRef:
                description: UserRef points at the ObjectStorageUser the key belongs
                  to. Immutable.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: userRef is immutable
                  rule: self == oldSelf
            required:
            - userRef
            type: object
          status:
            description: |-
              ObjectStorageAccessKeyStatus defines the observed state of
              ObjectStorageAccessKey.
            properties:
              accessKeyID:
                description: AccessKeyID as assigned by UpCloud.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: CreatedAt reported by the API, in RFC3339.
                type: string
              serviceUUID:
                description: ServiceUUID of the owning service.
                type: string
              username:
                description: Username the key belongs to.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
