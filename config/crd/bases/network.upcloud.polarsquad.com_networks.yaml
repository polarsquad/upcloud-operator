---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: networks.network.upcloud.polarsquad.com
spec:
  group: network.upcloud.polarsquad.com
  names:
    kind: Network
    listKind: NetworkList
    plural: networks
    singular: network
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.zone
      name: Zone
      type: string
    - jsonPath: .status.uuid
      name: UUID
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Network is an UpCloud SDN private network.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkSpec defines the desired state of an UpCloud SDN private
              network.
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy decides what happens to the UpCloud resource
                  when the CR is deleted.
                enum:
                - Delete
                - Orphan
                type: string
              ipNetworks:
                items:
                  description: IPNetwork is one subnet of an SDN network.
                  properties:
                    address:
                      description: Address in CIDR notation, for example 10.0.0.0/24.
                      minLength: 1
                      type: string
                    dhcp:
                      default: true
                      type: boolean
                    dhcpDefaultRoute:
                      default: false
                      type: boolean
                    dhcpDns:
                      items:
                        type: string
                      type: array
                    dhcpRoutes:
                      items:
                        type: string
                      type: array
                    family:
                      default: IPv4
                      enum:
                      - IPv4
                      - IPv6
                      type: string
                    gateway:
                      description: Gateway address. UpCloud picks one when omitted.
                      type: string
                  required:
                  - address
                  type: object
                minItems: 1
                type: array
              labels:
                additionalProperties:
                  type: string
                description: |-
                  UpCloudLabels is the user-controlled part of the labels set on an UpCloud resource.
                  Keys managed-by and k8s-uid are reserved for the operator.
                type: object
              name:
                description: Name in UpCloud. Defaults to metadata.name.
                maxLength: 255
                type: string
              routerRef:
                description: RouterRef attaches the network to a Router CR in the
                  same namespace.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              routerUUID:
                description: RouterUUID attaches the network to an existing UpCloud
                  router by UUID.
                type: string
              zone:
                description: Zone, for example fi-hel1. Immutable.
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: zone is immutable
                  rule: self == oldSelf
            required:
            - ipNetworks
            - zone
            type: object
          status:
            description: NetworkStatus defines the observed state of Network.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              routerUUID:
                description: RouterUUID currently attached in UpCloud.
                type: string
              uuid:
                description: UUID of the network in UpCloud.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
